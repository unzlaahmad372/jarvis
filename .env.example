# JARVIS Configuration — copy to .env and fill in values
# Never commit .env to Git

# ── LLM ──────────────────────────────────────────────────────────────────────
JARVIS_LLM_PROVIDER=ollama
JARVIS_LLM_MODEL=llama3.2
JARVIS_OLLAMA_URL=http://127.0.0.1:11434
JARVIS_ALLOW_CLOUD=false

# ── Embedding ─────────────────────────────────────────────────────────────────
JARVIS_EMBEDDING_MODEL=nomic-embed-text

# ── Vision (Phase 14) ─────────────────────────────────────────────────────────
JARVIS_VISION_MODEL=llava
JARVIS_ENABLE_VISION=true

# ── MCP (Phase 13) ────────────────────────────────────────────────────────────
# JSON array of MCP server configs — leave empty to disable
JARVIS_MCP_SERVERS=

# ── Context / token budgets ───────────────────────────────────────────────────
JARVIS_MAX_CONTEXT_TOKENS=8192
JARVIS_MAX_RESPONSE_TOKENS=2048
JARVIS_RECENT_HISTORY_TOKENS=2048
JARVIS_RAG_CONTEXT_TOKENS=3072
JARVIS_MEMORY_CONTEXT_TOKENS=512

# ── Inference guardrails ──────────────────────────────────────────────────────
JARVIS_MAX_CONCURRENT_LLM_REQUESTS=2
JARVIS_LLM_REQUEST_TIMEOUT=120
JARVIS_MAX_QUEUE_LENGTH=10
JARVIS_MAX_TOOL_OUTPUT_SIZE=16384
JARVIS_MAX_RAG_CHUNKS=10

# ── Data ──────────────────────────────────────────────────────────────────────
JARVIS_DATA_DIR=./data
JARVIS_DATABASE_URL=sqlite+aiosqlite:///./data/database/jarvis.db

# ── Server ────────────────────────────────────────────────────────────────────
JARVIS_HOST=127.0.0.1
JARVIS_PORT=8000
JARVIS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# ── Logging ───────────────────────────────────────────────────────────────────
JARVIS_LOG_LEVEL=INFO

# ── Runtime ───────────────────────────────────────────────────────────────────
# native = local dev, container = Docker
JARVIS_RUNTIME_MODE=native

# ── Safety ───────────────────────────────────────────────────────────────────
# false = LOW_RISK tools run without confirmation; SENSITIVE/DANGEROUS always confirm.
JARVIS_REQUIRE_CONFIRMATION=false

# ── Filesystem extra roots ────────────────────────────────────────────────────
# Comma-separated paths to allow file tools to access beyond ./data/
# Example: JARVIS_EXTRA_FILE_ROOTS=C:/Users/me/Documents,D:/projects
JARVIS_EXTRA_FILE_ROOTS=

# ── Allowed applications ────────────────────────────────────────────────────
# Comma-separated extra app names to add to the open_application tool allowlist
# Example: JARVIS_ALLOWED_APPS=obsidian,spotify,gitkraken
JARVIS_ALLOWED_APPS=

# ── Retention ────────────────────────────────────────────────────────────────
JARVIS_CONVERSATION_RETENTION_DAYS=365
JARVIS_TOOL_LOG_RETENTION_DAYS=90
JARVIS_TEMP_RETENTION_HOURS=24

# ── Auth (Phase 11) ───────────────────────────────────────────────────────────
# MUST be overridden in production — use a long random secret
JARVIS_AUTH_SECRET_KEY=change-me-in-production-use-a-long-random-secret

# ── Feature flags (all off by default) ───────────────────────────────────────
JARVIS_ENABLE_CLOUD=false
JARVIS_ENABLE_SHELL=false
JARVIS_ENABLE_K8S_WRITE=false
JARVIS_ENABLE_REMOTE_ACCESS=false
JARVIS_ENABLE_ALWAYS_LISTENING=false
JARVIS_INBOX_WATCHER_ENABLED=false

# ── Web Search (Phase 36) ─────────────────────────────────────────────────────
# Uses DuckDuckGo HTML endpoint — no API key required
JARVIS_ENABLE_WEB_SEARCH=false

# ── Calendar (Phase 38) ───────────────────────────────────────────────────────
# Export your calendar as .ics and point JARVIS at it
JARVIS_ENABLE_CALENDAR=false
JARVIS_CALENDAR_ICS_PATH=

# ── Docker / container overrides (uncomment when using docker-compose) ────────
# JARVIS_RUNTIME_MODE=container
# JARVIS_HOST=0.0.0.0
# JARVIS_OLLAMA_URL=http://ollama:11434
# JARVIS_ENABLE_REMOTE_ACCESS=true
